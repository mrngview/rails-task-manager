<%= form_for(@task) do |f| %>

  <%= f.label :name, "Enter a name" %>
  <%= f.text_field :name %>

  <%= f.label :description, "Enter an description" %>
  <%= f.text_field :description %>

  <%= f.submit %>
<% end %>
